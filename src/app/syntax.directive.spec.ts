import { SyntaxDirective } from './syntax.directive';

describe('SyntaxDirective', () => {
  it('should create an instance', () => {
    const directive = new SyntaxDirective();
    expect(directive).toBeTruthy();
  });
});
